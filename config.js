/* 富士山への道 —— 前端設定
 *
 * 這個檔案會跟著網站一起公開，所以「不要」把任何金鑰或網址放進來。
 *
 * 後端網址（Apps Script）不寫在這裡，改成第一次開啟網站時貼進去，存在瀏覽器本機。
 * 這樣 repo 可以公開，而那把能讀寫訓練資料的鑰匙不會留在 git 歷史裡。
 *   設定方式：開啟網站 → 點右上角的同步標籤 → 貼上網址
 *   或直接開：https://<你的帳號>.github.io/<repo>/#gas=<你的Apps Script網址>
 */
window.FUJI_CONFIG = {

  // 自架在私人環境、確定不會公開時才填。公開部署請留空。
  gasUrl: "",

  // 只有「直接雙擊本機 HTML 檔」才會用到，讓網頁自己去打氣象署。
  // 這個欄位由 build-local.py 自動注入離線版，這裡永遠留空。
  cwaKeyForLocalFileOnly: ""

};
